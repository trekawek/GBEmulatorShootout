name: CI - SuperSnes9x

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    uses: ./.github/workflows/emulator-runner.yml
    with:
      emulator: SuperSnes9x
      needs_audio: true

  deploy-pages:
    needs: run
    uses: ./.github/workflows/deploy-pages.yml
